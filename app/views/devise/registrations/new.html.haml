.container.general-main-wrap
  .text-center
    %h3.general-login-title Sign up
    %a.general-login-icon{href: "#"}
      %i.fa.fa-facebook-official
    %p.general-login-text or

  = general_simple_form_for resource, as: resource_name,
    url: registration_path(resource_name) do |field|

    = render layout: 'fields', locals: { field: field } do
      .form-group.mb-30
        = field.input :password_confirmation, autocomplete: "off"


  %p.general-sign-text.text-center
    Do you have account?
    = link_to "Log in", new_session_path(resource_name), class: 'in-gold-500'
